<!DOCTYPE html>
<html>
  <head>
    <title>TodoLabSim</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
      <div class="container">
        <a class="navbar-brand" href="<%= root_path %>">Todo Lab</a>
        <div class="collapse navbar-collapse">
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <%= link_to "Todos", todos_path, class: "nav-link #{current_page?(todos_path) ? 'active' : ''}" %>
            </li>
            <li class="nav-item">
              <%= link_to "Lab Console", lab_path, class: "nav-link #{current_page?(lab_path) ? 'active' : ''}" %>
            </li>
          </ul>
          <% if lab_mode? %>
            <span class="badge bg-warning text-dark">Simulation Active</span>
          <% else %>
            <span class="badge bg-secondary">Simulation Inactive</span>
          <% end %>
        </div>
      </div>
    </nav>

    <div class="container">
      <% flash.each do |key, message| %>
        <div class="alert alert-<%= key == "alert" ? "warning" : key == "lab_notice" ? "info" : "success" %>">
          <%= message %>
        </div>
      <% end %>
      <%= yield %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9j3f4/2LcOYf2IrZKttlfO4z7q2I0CYNnN0Qw2Q" crossorigin="anonymous"></script>
  </body>
</html>
